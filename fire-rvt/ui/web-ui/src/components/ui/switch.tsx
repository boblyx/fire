import * as React from "react"
import * as Switch from "@radix-ui/react-switch";
export let test = 0;
/*
const AISwitch = React.forwardRef<Reach.ElementRef<typeof Switch.Root>, React.ComponentPropsWithoutRef<typeof Switch.Root></typeof>({

})

*/
